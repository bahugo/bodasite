---
interface Props {
    title: string;
}
import '@fontsource/twinkle-star';
// import { Font } from "astro:assets";
const { title } = Astro.props;
---

<!-- <Font cssVariable="--font-roboto" preload /> -->

<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="description" content="Boda agence architectes" />
        <meta name="viewport" content="width=device-width" />
        <meta name="generator" content={Astro.generator} />
        <title>{title}</title>
    </head>
    <body>
        <slot />
    </body>
</html>
<style is:global>
    :root {
        --accent: 136, 58, 234;
        --accent-light: 224, 204, 250;
        --accent-dark: 49, 10, 101;
        --accent-gradient: linear-gradient(
            45deg,
            rgb(var(--accent)),
            rgb(var(--accent-light)) 30%,
            white 60%
        );
    }
    html {
        font-family: "Roboto";
        background: #fffbfb;
        background-size: 224px;
    }
    main {
        margin: auto;
        padding: 1rem;
        width: 800px;
        max-width: calc(100% - 2rem);
        color: black;
        font-size: 20px;
        line-height: 1.6;
    }
    h1 {
        font-size: 3rem;
        font-weight: 500;
        line-height: 1;
        text-align: center;
        margin-bottom: 1em;
    }
    p {
        font-size: 1rem;
        line-height: 1;
        text-align: center;
        margin-bottom: 1em;
    }
</style>
